# CardioSense AI — Backend Production Requirements
# Python 3.11.11 (set in Render dashboard AND runtime.txt)
#
# Intentionally excluded:
#   torch         — ECG model is .keras (TensorFlow), not PyTorch
#   numba         — not used in production
#   llvmlite      — pulled in by numba, also not used
#   cloudpickle   — not used
#   grpcio        — TF internal, installed by tensorflow transitively

# ── Web framework ─────────────────────────────────────────────
fastapi==0.115.0
granian==2.5.0
python-multipart==0.0.9
starlette==0.38.6

# ── Auth ──────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# ── Database ──────────────────────────────────────────────────
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2

# ── Pydantic / settings ───────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2
pydantic[email]==2.9.2
email-validator==2.3.0

# ── HTTP client (CardioBot Groq streaming) ────────────────────
httpx==0.28.1

# ── ML — clinical branch ──────────────────────────────────────
scikit-learn==1.6.1
xgboost==2.1.1
joblib==1.4.2
shap==0.46.0

# ── ML — ECG branch ───────────────────────────────────────────
tensorflow==2.18.0
keras==3.15.0
numpy==1.26.4

# ── Data processing ───────────────────────────────────────────
pandas==2.2.2
scipy==1.13.1

# ── ECG image digitizer (app/services/ecg_digitizer.py) ──────
opencv-python-headless==4.10.0.84
Pillow==10.4.0
